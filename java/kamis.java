

// class Node {
//     int data;
//     Node sebelum, selanjutnya;
//     Node(int d) { 
//         nilai = d; }
// }

public class kamis {
    // Node head

    // public void hapusNode(Node target) {
    //     if (head == null || target == null) return;

    //     if (head == target) {
    //         head = target.selanjutnya;
    //     }

    //     if (target.sebelumnya != null) { }
    // }
    public static void main(String[] args) {
        int a, b;
        a = 1;
        b = 2;
        System.out.printf("a + b = %d", a + b);
    }
}
